#include "main.h"
/**
 * _islower - check @c, if its a lowercase returns 1 otherwise returns 0
 * @c :character that needs to be checked
 * Return:0 or 1
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
