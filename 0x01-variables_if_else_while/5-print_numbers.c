#include <stdio.h>
/**
 * main - Starting point
 * A program that prints all digit from zero to 10
 * Return: 0
 */

int main(void)

{

	int no;

	for (no = 0; no < 10; no++)
	{
		printf("%i", no);
	}

	printf("\n");

	return (0);

}
